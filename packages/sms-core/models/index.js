'use strict';

const smsCategories = require('./sms_categories');
const smsProviderPresets = require('./sms_provider_presets');
const smsProviderTrackers = require('./sms_provider_trackers');
const smsSenders = require('./sms_senders');
const smsInsightNumbers = require('./sms_insight_numbers');

module.exports = {
  smsCategories,
  smsProviderPresets,
  smsProviderTrackers,
  smsSenders,
  smsInsightNumbers
};
